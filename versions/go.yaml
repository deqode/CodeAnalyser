name: go

versions:
  - ">=2.x"
  - "1.6"

framework:
  - name: gin
    versions:
      - name: "v1.x"
        semver: ">=1.x,<=2.x"

  - name: beego
    versions:
      - name: "v1.8"
        semver: ">=1.x,<=2.x"
      - name: "v2"
        semver: ">=1.x,<=2.x"

databases:
  - name: postgress
    versions:
      - name: "v12"
        semver: ">=11.x,<=12.x"
      - name: "v1.9.18"
        semver: ">=1.x,<=2.x"

orms:
  - name: gorm
    versions:
      - name: "v1.9.18"
        semver: ">=1.x,<=2.x"
      - name: "v1.9.18"
        semver: ">=1.x,<=2.x"

dependencies:
libraries: