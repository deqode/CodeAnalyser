name: go

versions:
  - ">=2.x"
  - "1.6"

framework:

  - name: gin
    version:
      - "^1.6"
      - ">=2.x"

  - name: beego
    version:
      - "^1.3"
      - ">=2.x"

databases:
  - name: postgress
    version:
      - "12"
      - "10"

orms:
  - name: gorm
    version:
      - "~3.x"

dependencies:

libraries: